apply plugin: 'java'

file('gradle.d').list().each {
    it ==~ /.+\.gradle/ && apply(from: "gradle.d/$it")
}

group='taosha.util'
version='1.0.1'

sourceCompatibility = 1.6
targetCompatibility = 1.6

repositories {
	jcenter()
}

dependencies {
	provided 'com.google.android:android:2.+'
}
